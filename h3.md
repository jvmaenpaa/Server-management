## x) Read & summarize

In the articles I have listed below, handlers and ansible.builtin.service module are introduced. Handlers are tasks that run only when something has changed, like a config file of a service. And the ansible.builtin.service is used by the handlers to manage a service. 
Let´s imagine that I have just done some changes to a apache2 configuration file and I would like to restart it by using a handler. The handler would use the ansible.builtin.service module to make the restart happen. Of course there is a lot more to it than what I just said but I hope this gives you the basic idea of what these components can do.

https://terokarvinen.com/apache-ansible/
https://docs.ansible.com/projects/ansible/latest/playbook_guide/playbooks_handlers.html
ansible-doc service

## a) Apassi (install apache2 manually and make your site to be seen on server starting page. Page must be editable without sudo or root access. 

First I installed apache with `sudo apt-get install apache2`. Then I enabled at made sure it was running. 

<img width="1079" height="325" alt="image" src="https://github.com/user-attachments/assets/874d2828-07d9-4637-ba48-3f14f2d56245" />
<img width="867" height="788" alt="image" src="https://github.com/user-attachments/assets/9b703f62-a95b-4825-99a7-667637c3ee63" />

Then I made the new virtual host `sudoedit /etc/apache2/sites-available/joni.com.conf`

<img width="608" height="196" alt="Screenshot 2026-04-14 144638" src="https://github.com/user-attachments/assets/6696130d-fa2c-43ba-bba9-d0cf11dc22b7" />

and enabled it with `sudo a2ensite joni.com`. Then I restarted apache2 to make this changes take place. 
Now was time to create the directory for the web page and make the index.html file. 

I just copied short html page from [Tero's page](https://terokarvinen.com/2012/short-html5-page/), but just changed the content a bit. 
<img width="402" height="246" alt="Screenshot 2026-04-14 145251" src="https://github.com/user-attachments/assets/2b907961-9fa0-4638-8b88-7e0305e85005" />

I also had to add the new virtual host to the /etc/hosts file so I could try and see if all the previous steps have made fruit.
Then I tried to call the site but I had forgotten to enable the site so I had to do that and restart apache to try again. 
<img width="615" height="228" alt="Screenshot 2026-04-14 150045" src="https://github.com/user-attachments/assets/047c22cb-372d-4901-9035-42a7b1467bb1" />
<img width="611" height="306" alt="Screenshot 2026-04-14 150119" src="https://github.com/user-attachments/assets/5d174085-774b-4735-88c3-06d4f7362fe2" />

As you can see, changes were made but I had a problem with permissions. 

<img width="1900" height="283" alt="Screenshot 2026-04-14 150254" src="https://github.com/user-attachments/assets/f3726b20-0df1-45fe-8a08-668a42c1d517" />

I tried couple of command to give permissions to the directory and the index.html file but nothing worked and then I realised I had to give 'others' execution rights to my home directory so Apache could enter it. So I ran the command `sudo chmod o+x /home/joni` and after that the page opened.

<img width="568" height="220" alt="image" src="https://github.com/user-attachments/assets/6cf3819d-d874-4eb3-9eee-67682da9b56b" />







      
